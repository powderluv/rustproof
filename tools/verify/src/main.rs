#![allow(unused)]

//! verify -- run Verus over the TCB crates with the pinned Z3 (CI gate). Host-side (std). TODO: implement. See docs/dev-infra.md.

fn main() {
    eprintln!("verify: not yet implemented (pre-M0 scaffold). See docs/.");
}
