#![allow(unused)]

//! run-vm -- wrap libvirt/start-rustproof-vm.sh to boot the ISO with GPU passthrough. Host-side (std). TODO: implement. See docs/dev-infra.md.

fn main() {
    eprintln!("run-vm: not yet implemented (pre-M0 scaffold). See docs/.");
}
