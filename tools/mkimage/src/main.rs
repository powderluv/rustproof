#![allow(unused)]

//! mkimage -- build the Limine boot ISO (nucleus + init + driver-host). Host-side (std). TODO: implement. See docs/dev-infra.md.

fn main() {
    eprintln!("mkimage: not yet implemented (pre-M0 scaffold). See docs/.");
}
